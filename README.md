# dummy-website

Simple Cloudflare-compatible static webpage with 6 dashboard blocks:

- Top row (3): Total Tasks Completed, Active Agents, Tokens Used
- Bottom row (3): Tokens Left, Today Completed, Queue Backlog

## Deploy on Cloudflare Pages

- Framework preset: **None**
- Build command: *(leave empty)*
- Build output directory: `/`

This repo is plain static HTML (`index.html`), so no install/build is needed.
